/**
 * 
 * 定性评估FChart
 */

Ext.define('FHD.view.risk.assess.utils.TempContainer', {
    extend: 'Ext.container.Container',
    alias: 'widget.tempContainer',
    
    // 初始化方法
    initComponent: function() {
    	var me = this;
    	
        Ext.apply(me, {
        	
        });

        me.callParent(arguments);
    }

});